# Constructor ship handover (2026-08-23)

## 1. Start here - runnable, in order

1. `curl.exe -s -o NUL -w "pages=%{http_code}`n" https://netie-ai.github.io/constructor/`
   Proves Pages sketch is up. ~5s. Does not prove live DAG run.

2. `curl.exe -s -o NUL -w "prod=%{http_code}`n" https://app.netie.ai/cortex/`
   Proves production engine path. ~5s. 404 means strangers cannot live-run.

3. `curl.exe -s -o NUL -w "8012=%{http_code}`n" http://127.0.0.1:8012/cortex/login`
   Proves local constructor-mount login page. ~2s. Needs the 8012 uvicorn from NEXT_CHAT.md.

4. `gh pr view 14 --repo Netie-AI/landing --json state,url`
   Proves whether the Next homepage CTA has landed. Does not prove netie.ai FTP.

5. From `D:\Cortex-constructor-mount`: `python -m pytest tests/dms/test_constructor_graph.py -q --tb=line`
   Proves compile/fetch/run routes in tests. Does not prove app.netie.ai.

## 2. What changed

Inspect fields were labels only.
Constructor customize now seeds DuckDB via `packs/dms/constructor_fetch.py` and F8 `export_pptx` on `POST /cortex/constructor/run`. Chat `fetch` / `run all` have no silent ghost fallback.
Stops at: proven on local 8012 and pytest, not on app.netie.ai.

Chat was imprecise / rebuilt the foundry path on unknown text.
Unknown text now refuses. `bench` cites G1 DAG 4.1 vs OpenClaw 1.7 plus live row count when on `/cortex`.
Stops at: not a live OpenClaw rerun.

Cortex login HTML form 500 without python-multipart.
Login posts JSON to `/cortex/session`. Merged in Cortex PR 53 then fetch work in PR 54 (`0cb485f`).
Stops at: host still empty LiteSpeed autoindex.

OPEN CONSTRUCTOR was a dead button; PR 11 only patched CRA `App.js`.
Next `app/page.tsx` is what `next build` exports. Landing PR 14 adds an `<a>` CTA + Constructor card + footer. Export at `D:\landing-ftp-export\netie-agent\out\`.
Stops at: not on live netie.ai until FTP. Do not merge landing from the Constructor repo.

## 3. Deliberately not fixed

app.netie.ai has no Cortex process. Root is empty LiteSpeed autoindex. Needs a human with Spaceship/VPS access. Ruled out: this laptop has no FTP/SSH keys, no FileZilla, no gh secrets.

Local 8010 old pack left running (no constructor). 8012 is the constructor-mount. Do not kill 8010 casually.

P16 WhatsApp send, P1 / O6 / P17 stay parked.

## 4. Numbers (measured 2026-08-23 ~12:45 +08)

| Gate | Result | Where |
| Pages constructor | HTTP 200 | curl github.io |
| app.netie.ai/cortex | HTTP 404 | curl |
| Local 8012 /cortex/login | HTTP 200 | curl |
| netie.ai OPEN CONSTRUCTOR | dead `<button>` | python scrape |
| Cortex PR 54 | MERGED 0cb485f | gh |
| Landing PR 14 | OPEN | gh |
| pytest constructor_graph + accuracy | 20 passed (earlier this session) | local pytest, not re-run at handover |

## 5. Corrections

PR 11 did not fix production. Live site is the Next export, not CRA App.js.

`D:\Cortex\.venv` uvicorn failed with `openai.types.responses.response_function_call_output_item_list`. Local 8012 was started with Python 3.14.

G1 4.1 vs OpenClaw 1.7 is a repo bakeoff, not a live OpenClaw host.

## 6. What needs you

1. Merge landing PR 14 from a landing worktree if CI is green (not from Constructor).
2. FTP `D:\landing-ftp-export\netie-agent\out\` to Spaceship `public_html/` (~20 min).
3. Install Cortex main on the app.netie.ai host and reverse-proxy `/cortex` to uvicorn (~20 min). Issue OpenVault `ov_` keys.
4. Click OPEN CONSTRUCTOR on https://netie.ai/ and confirm it leaves netie.ai. Then hit app.netie.ai/cortex login.
5. Keep landing#8 and #9 open until 4 is true.
