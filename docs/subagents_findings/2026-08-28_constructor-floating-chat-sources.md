```yaml
keywords: [constructor, chat, dock, source, cloud, database, refuse, palantir]
main_idea: "Chat is a snap dock (right/bottom, Ctrl+/, FAB). Connector/ingest bind place, ghost cloud sign-in, or db link. Compiles whole ingest->app. Still refuses CCTV/face/stalk/sex-work. Incidents object is owned case rows only."
models: [grok-4.6]
workflow: none
reuse: golden_rule
status: raw
cite: agent: constructor-floating-chat-sources
repo: Constructor
date: 2026-08-28
```

# Floating chat + source kinds

PREFLIGHT: HIT constructor-calendar-ingest-skin

## Main idea

- Chat left the 180px footer strip. Dock snaps right or bottom. Ctrl+/ toggles. Esc closes. FAB when closed.
- Palantir-style mock: name a source (place table, ghost cloud sign-in, database link) then compile ingest -> connector -> ontology -> insight -> foundry -> app.
- `incidents` is owned case rows (`db.incidents`), not cameras.
- Refuse CCTV, cameras, face match, stalking, sex-work.

## Verify

Open skin. Chat dock on the right. Ctrl+/ hides it and shows FAB. Snap Bottom. Chat `database link db.incidents then foundry app` compiles incidents hop 0. Chat a CCTV/face line refuses and leaves the graph.

## Do not

- Compile CCTV, face similarity, or people-spotting
- Real OAuth or a live DB driver on Pages
- Clone Foundry / n8n
