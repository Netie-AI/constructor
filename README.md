# Constructor

Consumer skin for the Cortex engine. Ticket: [landing#9](https://github.com/Netie-AI/landing/issues/9).

- Sketch (no login, no `fetch`): https://netie-ai.github.io/constructor/
- Power: Cortex on `http://127.0.0.1:8010/constructor/` once the Cortex writer mounts it. Not a new hostname.

ChatGPT-style box compiles the canvas. Ghost mode dry-runs (no writes). Propose 3 uses Cortex coordination patterns (single agent, generator-verifier, orchestrator-subagent). Maximize applies the winner. Foundry path is connector -> ontology -> insight -> foundry -> app. Ontology pack is a stub until you create one.

Do not invent `constructor.netie.ai`. Do not merge landing. Do not clone n8n/Activepieces.
